import { WebNativeElement } from './web-native-elements/base/WebNativeElement';
export class VarBind {
  value: WebNativeElement
  constructor() {
    this.value = undefined as unknown as WebNativeElement
  }
}